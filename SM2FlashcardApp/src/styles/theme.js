/**
 * Combined theme object for the SM2 Flashcard App
 */

import { colors } from './colors';
import { typography, fontFamily, fontSize } from './typography';
import { spacing, borderRadius, shadows, shadowToBoxShadow } from './spacing';

const theme = {
  colors,
  typography,
  fontFamily,
  fontSize,
  spacing,
  borderRadius,
  shadows,
  shadowToBoxShadow,
};

export default theme;
